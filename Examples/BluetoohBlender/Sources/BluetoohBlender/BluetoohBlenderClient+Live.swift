// MARK: - BluetoohBlenderClient Live

public extension BluetoohBlenderClient {
    /// The live production implementation.
    /// Replace with a real CoreBluetooth implementation when needed.
    static func makeLive() -> Self {
        .noop
    }
}
